{
  "rules": {
    ".read": false,
    ".write": false,

    "presence": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['online','name','role','facility','email','lastSeen'])",
        "online": { ".validate": "newData.isBoolean()" },
        "name": { ".validate": "newData.isString() && newData.val().length <= 100" },
        "role": { ".validate": "newData.isString() && newData.val().length <= 60" },
        "facility": { ".validate": "newData.isString() && newData.val().length <= 120" },
        "email": { ".validate": "newData.isString() && newData.val().length <= 254" },
        "lastSeen": { ".validate": true },
        "$other": { ".validate": false }
      }
    },

    "chats": {
      "$convId": {
        ".read": "auth != null",
        "messages": {
          ".read": "auth != null",
          "$msgId": {
            ".write": "auth != null && !data.exists()",
            ".validate": "newData.hasChildren(['from','text','ts'])",
            "from": { ".validate": "newData.isString() && newData.val().length <= 254" },
            "fromName": { ".validate": "newData.isString() && newData.val().length <= 100" },
            "text": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 2000" },
            "ts": { ".validate": true },
            "fileName": { ".validate": "newData.isString() && newData.val().length <= 260" },
            "fileData": { ".validate": "newData.isString() && newData.val().length <= 700000" },
            "fileType": { ".validate": "newData.isString() && newData.val().length <= 120" },
            "fileSize": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 512000" },
            "$other": { ".validate": false }
          }
        }
      }
    },

    "calls": {
      "$callId": {
        ".read": "auth != null",
        ".write": "auth != null",
        "status": { ".validate": "newData.isString() && (newData.val() === 'ringing' || newData.val() === 'accepted' || newData.val() === 'declined' || newData.val() === 'ended')" },
        "callerEmail": { ".validate": "newData.isString() && newData.val().length <= 254" },
        "calleeEmail": { ".validate": "newData.isString() && newData.val().length <= 254" },
        "callerName": { ".validate": "newData.isString() && newData.val().length <= 100" },
        "calleeName": { ".validate": "newData.isString() && newData.val().length <= 100" },
        "createdAt": { ".validate": true },
        "offer": {
          ".validate": "newData.hasChildren(['type','sdp'])",
          "type": { ".validate": "newData.isString()" },
          "sdp": { ".validate": "newData.isString() && newData.val().length <= 20000" }
        },
        "answer": {
          ".validate": "newData.hasChildren(['type','sdp'])",
          "type": { ".validate": "newData.isString()" },
          "sdp": { ".validate": "newData.isString() && newData.val().length <= 20000" }
        },
        "callerCandidates": {
          ".read": "auth != null",
          "$candId": {
            ".write": "auth != null && !data.exists()",
            ".validate": "newData.hasChild('candidate')"
          }
        },
        "calleeCandidates": {
          ".read": "auth != null",
          "$candId": {
            ".write": "auth != null && !data.exists()",
            ".validate": "newData.hasChild('candidate')"
          }
        }
      }
    },

    "incomingCalls": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "!newData.exists() || newData.hasChildren(['callId','callerEmail'])"
      }
    },

    "messageNotify": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "!newData.exists() || newData.hasChildren(['convId','fromEmail','text','ts'])"
      }
    },

    "callLogs": {
      "$uid": {
        ".read": "auth != null",
        "$logId": {
          ".write": "auth != null && !data.exists()",
          ".validate": "newData.hasChildren(['direction','status','startedAt'])",
          "peerEmail": { ".validate": "newData.isString() && newData.val().length <= 254" },
          "peerName": { ".validate": "newData.isString() && newData.val().length <= 100" },
          "direction": { ".validate": "newData.isString() && (newData.val() === 'incoming' || newData.val() === 'outgoing')" },
          "status": { ".validate": "newData.isString() && (newData.val() === 'completed' || newData.val() === 'missed' || newData.val() === 'declined' || newData.val() === 'no-answer')" },
          "startedAt": { ".validate": true },
          "durationSec": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 86400" },
          "$other": { ".validate": false }
        }
      }
    },

    "userConversations": {
      "$uid": {
        ".read": "auth != null",
        "$convId": {
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['peerEmail','lastMessage','lastFrom','ts'])",
          "peerEmail": { ".validate": "newData.isString() && newData.val().length <= 254" },
          "peerName": { ".validate": "newData.isString() && newData.val().length <= 100" },
          "lastMessage": { ".validate": "newData.isString() && newData.val().length <= 2000" },
          "lastFrom": { ".validate": "newData.isString() && newData.val().length <= 254" },
          "ts": { ".validate": true },
          "$other": { ".validate": false }
        }
      }
    }
  }
}
